import React, { useContext } from 'react';
import { AuthContext } from '../Context/AuthProvider';

const Home = () => {
    
    return (
        <div>
            This is HOME
        </div>
    );
};

export default Home;